import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  const 
  return (
    <View style={styles. styleCon}>
      <Text>Love you Dhiti</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  styleCon :{
    padding: 2,
    borderWidth: 2,
    borderColor: 'blue'
  }
});
